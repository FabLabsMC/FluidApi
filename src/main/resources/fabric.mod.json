{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "FabLabs ${libname} version ${libver}",
  "icon": "assets/blueprint/icon.png",
  "description": "Fluid stack reference implementation for Fabric API",
  "license": "MIT",
  "contact": {
    "sources": "https://github.com/${repo}"
  },
  "environment": "*",
  "entrypoints": {
    "main": [
      "io.github.fablabsmc.fablabs.api.fluidstack.v1.FluidStacks"
    ]
  },
  "depends": {
    "fabricloader": ">=0.4.0"
  },
  "custom": {
    "modmenu:api": true
  }
}
